import { FeesSearchPipe } from './fees-search.pipe';

describe('FeesSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new FeesSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
