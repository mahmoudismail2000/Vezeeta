import { SeachSpecialtiesPipe } from './seach-specialties.pipe';

describe('SeachSpecialtiesPipe', () => {
  it('create an instance', () => {
    const pipe = new SeachSpecialtiesPipe();
    expect(pipe).toBeTruthy();
  });
});
